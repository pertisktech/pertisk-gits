apiVersion: v1
kind: Service
metadata:
  name: {{ include "pertisk-gits.fullname" . }}
  labels:
    {{- include "pertisk-gits.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pertisk-gits.selectorLabels" . | nindent 4 }}
